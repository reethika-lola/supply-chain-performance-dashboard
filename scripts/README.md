# Scripts

profile_dataset.py is a standard-library Python profiler. It reads data/raw/DataCoSupplyChainDataset.csv without changing it and writes a local profile summary under data/processed.

Run:

python scripts/profile_dataset.py
