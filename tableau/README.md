# Tableau Build Package

A native workbook is intentionally not included because Tableau Desktop was unavailable in this environment. The calculations, source connection, worksheet specifications, and checklist are sufficient to build and validate it manually.

Connect Tableau to MySQL view supply_chain_analytics.vw_tableau_order_items, not to staging data.
